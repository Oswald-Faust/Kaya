# Decision log

| # | Date | Decision | Why | Revisit when |
|---|---|---|---|---|
| D-001 | 2026-09-13 | Modular monolith: Next.js 16 + Drizzle + PostgreSQL, no separate API service | Spec §19 baseline; one deployable keeps the growth loop's transactions simple | An engine needs isolation (crawler egress, long agent runs) |
| D-002 | 2026-09-13 | Drizzle over Prisma | Spec allows either; Drizzle keeps typed SQL close to the domain and supports jsonb typing and custom SQL migrations (audit trigger) | — |
| D-003 | 2026-09-13 | Local cookie-selected user instead of an auth provider | Spec leaves Clerk/Auth.js/Supabase open (§40 founder decision); tenancy is enforced independently of auth | Before any external user touches the app |
| D-004 | 2026-09-13 | Background work via Next `after()` + persisted run steps + client polling; no Redis or workflow engine yet | Analysis and strategy runs take seconds; persisted steps already make runs inspectable and resumable in principle. Spec §19 leaves Trigger.dev/Inngest/Temporal open | Runs exceed a request's max duration, or scheduled loops (daily brief, auto-pause monitor) are built |
| D-005 | 2026-09-13 | Deterministic heuristic extraction always runs; Claude (`claude-opus-5`, structured output, server-side fallbacks) refines when a key is configured | Works without credentials, gives an instant first result, and the model output is grounded against the same crawl | Extraction eval dataset shows the heuristic pass adds no value |
| D-006 | 2026-09-13 | Strategy generation is deterministic (channel fit + allocation + templates); no LLM strategy writing yet | Strategy must be explainable and tied to numbers; an LLM can later phrase it but not change scores | Strategy eval (§30) exists to measure LLM additions |
| D-007 | 2026-09-13 | Deterministic intent routing for the agent (diagnose/scale/allocate/grow) | Explicit, auditable handlers first (spec §15: no swarm); LLM planning plugs into the same tools and executor | Users ask for goals outside the four intents often |
| D-008 | 2026-09-13 | Demo workspace (Tickwarden, fictional cron-monitoring SaaS) generated from a causal model, not hand-typed numbers | Metrics, experiment results and learnings must agree (spec §21); a unit test guards the story | — |
| D-009 | 2026-09-13 | Integrations connect in explicit **demo mode** when no credentials exist | Spec §17: never pretend production APIs are connected; flows stay identical | OAuth apps for Stripe, Google, Meta are approved |
| D-010 | 2026-09-13 | Experiment evaluation: two-proportion z-test for rates (win ≥95% & lift ≥ threshold, lose ≥90%), Poisson test for CAC | Simple, dependency-free, conservative; results carry confidence | Sequential testing or Bayesian evaluation is needed for peeking |
| D-011 | 2026-09-13 | Memory influences ranking through similarity keys `channel:tactic:target`; losers with ≥0.7 confidence suppress the same channel+tactic | Directly answers "will the same failed experiment be recommended twice?" | Tactics need a taxonomy richer than a key |
| D-012 | 2026-09-13 | Goal "on track" uses recent weekly pace against required pace when pace is known, time-based progress otherwise | Straight-line time progress contradicted the brief for accelerating businesses | Goal metrics other than MRR get pace data |
| D-013 | 2026-09-13 | Autonomy mode names follow Notion (Observe/Suggest/Copilot/Autopilot), not the brief's five-level list | Notion is canonical; "Draft" and "Approve-to-run" map to Suggest and Copilot | — |
| D-014 | 2026-09-13 | Channel, SEO and Creators modules render as honest previews backed by real experiments on that channel | Build order (spec §31) puts them in S3/S5; empty tabs would be misleading | Those stages start |
| D-015 | 2026-09-15 | `/` is a public landing page; signed-in founders reach their workspace from "Open workspace" in the nav instead of an automatic redirect | The product needs a front door that explains the loop and starts analysis in one step. The page uses the real `StartForm`, Tickwarden demo numbers and the actual autonomy rules, and shows no invented customer logos or testimonials. Instrument Serif is used for display type on marketing surfaces only | Auth lands and a signed-in home is needed |
| D-016 | 2026-09-15 | Product renamed **Kaya**; identity modeled on Clay: light grounds, Host Grotesk + Geist Mono, a seven-accent clay palette, SVG clay illustrations; dark landing and serif display removed. App tokens retuned to warm neutrals with agent blue #2F56E8 | Founder decision. One family keeps marketing and product consistent; SVG illustrations stay light and themable without image generation | A designer produces rendered 3D illustrations |
| D-017 | 2026-09-15 | Real third-party logos appear only as integrations Kaya connects to; no customer logos, testimonials or stats that aren't from the demo workspace or enforced by code | The product has no customers yet; implying endorsements would be false | Real customers agree to be named |
| D-018 | 2026-09-15 | **Draft pricing**: Free / Launch $79+ / Growth $249+ / Scale custom, metered on agent actions (reads, briefs, blocked and failed actions free), 10% annual discount, no share of ad spend; plans cap managed spend | Mirrors Clay's usage model while matching Kaya's cost driver (tool calls). Numbers are a proposal for the founder to validate | Founder sets final prices, or billing is implemented |
| D-019 | 2026-09-15 | Home hero is a real-time 3D scene (three.js via @react-three/fiber + drei, loaded client-only, paused off-screen); two alternates kept at /lab/hero and ?hero=b / ?hero=c. Section motion uses `motion` for reveals and CSS keyframes for SVG illustrations, all respecting reduced motion | Founder asked for 3D and per-section motion; CSS keeps illustration motion cheap, R3F keeps 3D interactive without video assets | Performance budget on low-end mobile is exceeded |
| D-020 | 2026-09-15 | Two home page versions: `/` (3D Kaya mark hero) and `/v2` (Clay-style hero: real-time 3D clay machine with Rapier physics over a green field, product analysis on the right). Postprocessing removed after it blanked the canvas; grain and vignette are CSS. Marketing pages render without a database (bounded lookups) so they can deploy before the app has one | Founder asked for both directions to compare live | One version is chosen |
